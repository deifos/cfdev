interface:
  display_name: "cfdev"
  short_description: "Manage cfdev URLs, domains, and machine state"
  default_prompt: "Use $cfdev to safely manage my permanent Cloudflare URLs, active domain, and machine setup."
